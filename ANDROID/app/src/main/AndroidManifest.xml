<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yusuf.perpustakawan">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".view.splashScreen"
            android:theme="@style/Theme.PerpustaKawan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".view.Mainactivity"
            android:theme="@style/Theme.PerpustaKawan"/>

        <activity android:name=".view.listBook"
            android:theme="@style/Theme.PerpustaKawan"/>

        <activity android:name=".view.bPeminjaman"
            android:theme="@style/Theme.PerpustaKawan"/>

        <activity android:name=".view.detailBook"
            android:theme="@style/Theme.PerpustaKawan"/>

        <activity android:name=".view.akunActivity"
            android:theme="@style/Theme.PerpustaKawan"/>

    </application>

</manifest>